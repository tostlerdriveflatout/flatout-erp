'use client';import {useEffect,useMemo,useState} from 'react';import {supabase} from '@/lib/supabase-browser';import {useRouter} from 'next/navigation';
type Customer={
  id:string,
  name:string,
  company:string|null,
  email:string|null,
  phone:string|null,
  billing_address:string|null,
  shipping_address:string|null,
  notes:string|null
}; Product={id:string,sku:string|null,name:string,vendor:string|null,category:string|null,sell_price:number,cost:number|null,active:boolean};type Payment={id:string,order_id:string,amount:number,payment_method:string|null,reference:string|null,notes:string|null,paid_at:string};type Order={id:string,order_number:string,status:string,payment_status:string,tax_rate:number,customer_id:string,shipping_address:string|null,reference_number:string|null,created_at?:string,customers?:Customer,order_items?:Item[],payments?:Payment[],order_notes?:OrderNote[]};type PurchaseOrderItem={id:string,purchase_order_id:string,order_item_id:string|null,description:string,sku:string|null,qty:number,unit_cost:number|null,sort_order:number};type PurchaseOrder={id:string,po_number:string,order_id:string,vendor:string,notes:string|null,status:string,created_at:string,purchase_order_items?:PurchaseOrderItem[],orders?:Order};type OrderNote={id:string,order_id:string,note:string,created_at:string,created_by:string|null};type Item={id:string,order_id:string,product_id:string|null,item_type:string,description:string,sku:string|null,vendor:string|null,qty:number,sell_price:number,cost:number|null,purchasing_status:string,tracking:string|null,notes:string|null};
export default function Dashboard({email}:{email:string}){const s=supabase(),r=useRouter();const [tab,setTab]=useState('Dashboard'),[customers,setCustomers]=useState<Customer[]>([]),[products,setProducts]=useState<Product[]>([]),[orders,setOrders]=useState<Order[]>([]),[purchaseOrders,setPurchaseOrders]=useState<PurchaseOrder[]>([]),[selected,setSelected]=useState<Order|null>(null)[selectedPO,setSelectedPO]=useState<PurchaseOrder|null>(null),[editingCustomer,setEditingCustomer]=useState<Customer|null>(null),[modal,setModal]=useState(''),[search,setSearch]=useState('');async function load(){let [c,p,o,po]=await Promise.all([s.from('customers').select('*').order('name'),s.from('products').select('*').eq('active',true).order('name'),s.from('orders').select('*,customers(*),order_items(*),payments(*),order_notes(*)').order('created_at',{ascending:false}),s.from('purchase_orders').select('*,purchase_order_items(*),orders(*,customers(*))').order('created_at',{ascending:false})]);setCustomers(c.data||[]);setProducts(p.data||[]);setOrders((o.data||[]) as any);setPurchaseOrders((po.data||[]) as any)}useEffect(()=>{load()},[]);const need=orders.flatMap(o=>(o.order_items||[]).filter(i=>i.item_type==='Product'&&i.purchasing_status!=='Received'));const totals=(o:Order)=>{let sell=(o.order_items||[]).reduce((a,i)=>a+i.qty*Number(i.sell_price),0),cost=(o.order_items||[]).reduce((a,i)=>a+i.qty*Number(i.cost||0),0);return {sell,cost,gp:sell-cost,gm:sell?((sell-cost)/sell*100):0}};async function logout(){await s.auth.signOut();r.push('/login')}
async function syncPriceGuide(){
  if(!confirm('Sync the Product Catalog with the Google Price Guide?'))return;

  const {data:{session}}=await s.auth.getSession();

  if(!session?.access_token){
    alert('Your login session has expired. Please log in again.');
    return;
  }

  try{
    const response=await fetch('/api/sync-price-guide',{
      method:'POST',
      headers:{
        Authorization:`Bearer ${session.access_token}`
      }
    });

    const result=await response.json();

    if(!response.ok){
      alert(result.error||result.details||'Price Guide sync failed.');
      return;
    }

    await load();

alert(
  `Price Guide sync complete!\n\n`+
  `Added: ${result.added}\n`+
  `Updated: ${result.updated}\n`+
  `Skipped: ${result.skipped}`+
  (result.skipped_details?.length
    ? `\n\nSkipped Details:\n${result.skipped_details.join('\n')}`
    : '')+
  (result.errors?.length
    ? `\n\nErrors:\n${result.errors.join('\n')}`
    : '')
)
  }catch(error){
    console.error(error);
    alert('Price Guide sync failed. Please try again.');
  }
}
async function createCustomer(fd:FormData){await s.from('customers').insert({name:fd.get('name'),company:fd.get('company')||null,email:fd.get('email')||null,phone:fd.get('phone')||null});setModal('');load()}
  async function updateCustomer(fd:FormData){
  const id=String(fd.get('id'));
  const {error}=await s.from('customers').update({
    name:fd.get('name'),
    company:fd.get('company')||null,
    email:fd.get('email')||null,
    phone:fd.get('phone')||null,
    billing_address:fd.get('billing_address')||null,
    shipping_address:fd.get('shipping_address')||null,
    notes:fd.get('notes')||null
  }).eq('id',id);

  if(error){
    alert(error.message);
    return;
  }

 setModal('');
setEditingCustomer(null);
await load();
}                                                        
async function createOrder(fd:FormData){let customer=String(fd.get('customer_id')),tax=Number(fd.get('tax_rate')||0),reference=String(fd.get('reference_number')||'').trim();let {data}=await s.from('orders').insert({customer_id:customer,reference_number:reference||null,tax_rate:tax,status:'Draft',payment_status:'Not Invoiced'}).select('*,customers(*),order_items(*),payments(*),order_notes(*)').single();setModal('');await load();if(data){setSelected(data as any);setTab('Order')}}
async function addProduct(pid:string){if(!selected)return;let p=products.find(x=>x.id===pid);if(!p)return;await s.from('order_items').insert({order_id:selected.id,product_id:p.id,item_type:'Product',description:p.name,sku:p.sku,vendor:p.vendor,qty:1,sell_price:p.sell_price,cost:p.cost,purchasing_status:'Need to Order'});await refreshOrder()}
async function addOther(fd:FormData){if(!selected)return;let type=String(fd.get('item_type')),sell=Number(fd.get('sell_price')||0);if(type==='Discount'&&sell>0)sell=-sell;await s.from('order_items').insert({order_id:selected.id,item_type:type,description:fd.get('description'),qty:Number(fd.get('qty')||1),sell_price:sell,cost:fd.get('cost')?Number(fd.get('cost')):null,purchasing_status:'N/A'});setModal('');refreshOrder()}
async function refreshOrder(){if(!selected)return;let {data}=await s.from('orders').select('*,customers(*),order_items(*),payments(*),order_notes(*)').eq('id',selected.id).single();if(data)setSelected(data as any);load()}
async function dup(i:Item){let {id,...copy}=i as any;copy.tracking=null;if(copy.item_type==='Product')copy.purchasing_status='Need to Order';await s.from('order_items').insert(copy);refreshOrder()}
async function del(i:Item){if(!confirm(`Remove ${i.description}?`))return;await s.from('order_items').delete().eq('id',i.id);refreshOrder()}
async function updateItem(i:Item,k:string,v:any){await s.from('order_items').update({[k]:v}).eq('id',i.id);refreshOrder()}
async function updateShippingAddress(address:string){if(!selected)return;await s.from('orders').update({shipping_address:address||null}).eq('id',selected.id);await refreshOrder()}
async function updateReferenceNumber(reference:string){if(!selected)return;await s.from('orders').update({reference_number:reference.trim()||null}).eq('id',selected.id);await refreshOrder()}
async function updateOrderStatus(status:string){if(!selected)return;await s.from('orders').update({status}).eq('id',selected.id);await refreshOrder()}
async function addOrderNote(note:string){if(!selected||!note.trim())return;const {data:{user}}=await s.auth.getUser();await s.from('order_notes').insert({order_id:selected.id,note:note.trim(),created_by:user?.id||null});await refreshOrder()}
async function createPurchaseOrder(order:Order,items:Item[]){if(!order.reference_number){alert('Add a Reference # to this Sales Order before creating a PO.');return}if(!items.length)return;const vendors=[...new Set(items.map(i=>i.vendor||'').filter(Boolean))];if(vendors.length!==1){alert('A PO can only contain items from one vendor.');return}const vendor=vendors[0];let {data:po,error}=await s.from('purchase_orders').insert({po_number:order.reference_number,order_id:order.id,vendor,status:'Ordered'}).select('*').single();if(error||!po){alert(error?.message||'Could not create PO.');return}const snapshots=items.map((i,idx)=>({purchase_order_id:po.id,order_item_id:i.id,description:i.description,sku:i.sku,qty:i.qty,unit_cost:i.cost,sort_order:idx}));let {error:itemError}=await s.from('purchase_order_items').insert(snapshots);if(itemError){await s.from('purchase_orders').delete().eq('id',po.id);alert(itemError.message);return}await s.from('order_items').update({purchasing_status:'Ordered'}).in('id',items.map(i=>i.id));await load();let {data:full}=await s.from('purchase_orders').select('*,purchase_order_items(*),orders(*,customers(*))').eq('id',po.id).single();if(full){setSelectedPO(full as any);setSelected(null);setTab('Purchase Order')}}
async function openInvoice(){if(!selected)return;if(selected.payment_status==='Not Invoiced'){await s.from('orders').update({payment_status:'Invoiced'}).eq('id',selected.id);await refreshOrder()}setTab('Invoice')}
async function addPayment(fd:FormData){if(!selected)return;const amount=Number(fd.get('amount')||0);if(amount<=0)return;await s.from('payments').insert({order_id:selected.id,amount,payment_method:fd.get('payment_method')||null,reference:fd.get('reference')||null,notes:fd.get('notes')||null});let {data}=await s.from('orders').select('*,customers(*),order_items(*),payments(*),order_notes(*)').eq('id',selected.id).single();if(data){const sell=(data.order_items||[]).reduce((a:any,i:any)=>a+Number(i.qty)*Number(i.sell_price),0);const total=sell+(sell*Number(data.tax_rate||0)/100);const paid=(data.payments||[]).reduce((a:any,p:any)=>a+Number(p.amount),0);const status=paid>=total-.005?'Paid':paid>0?'Partially Paid':'Invoiced';await s.from('orders').update({payment_status:status}).eq('id',selected.id)}setModal('');await refreshOrder();setTab('Invoice')}
let filteredCustomers=customers.filter(c=>[c.name,c.company,c.email,c.phone].some(v=>(v||'').toLowerCase().includes(search.toLowerCase())));
return <div className="shell"><aside className="side"><img className="logo" src="/flatout-logo.svg"/><div className="nav">{['Dashboard','Customers','Products','Orders','Purchasing','Purchase Orders'].map(x=><button key={x} className={tab===x?'active':''} onClick={()=>{setTab(x);setSelected(null);setSelectedPO(null)}}>{x}</button>)}</div></aside><main className="main"><div className="top"><div><b>{selected?(tab==='Invoice'?`${selected.order_number} — Invoice`:selected.order_number):selectedPO&&tab==='Purchase Order'?`${selectedPO.po_number} — Purchase Order`:tab}</b><div className="muted">Flatout Sim Racing ERP</div></div><div className="row"><span className="muted">{email}</span><button className="btn secondary" onClick={logout}>Log out</button></div></div><div className="content">
{tab==='Dashboard'&&<><div className="cards"><div className="card"><div className="muted">Customers</div><div className="value">{customers.length}</div></div><div className="card"><div className="muted">Open Orders</div><div className="value">{orders.filter(o=>o.status!=='Completed').length}</div></div><div className="card"><div className="muted">Parts Awaiting</div><div className="value">{need.length}</div></div><div className="card"><div className="muted">Products</div><div className="value">{products.length}</div></div></div><div className="panel"><h2>Recent Orders</h2><table><thead><tr><th>Order</th><th>Customer</th><th>Status</th><th>Sell</th><th>Margin</th></tr></thead><tbody>{orders.slice(0,10).map(o=>{let t=totals(o);return <tr key={o.id} onClick={()=>{setSelected(o);setTab('Order')}} style={{cursor:'pointer'}}><td>{o.order_number}</td><td>{(o.customers as any)?.name}</td><td>{o.status}</td><td>${t.sell.toLocaleString()}</td><td>{t.gm.toFixed(1)}%</td></tr>})}</tbody></table></div></>}
{tab==='Customers'&&<><div className="row"><button className="btn" onClick={()=>setModal('customer')}>+ New Customer</button><input placeholder="Search customers" value={search} onChange={e=>setSearch(e.target.value)} style={{padding:9,width:320}}/></div><div className="panel"><h2>Customers</h2><table><tbody>{filteredCustomers.map(c=><tr key={c.id}>
  <td><b>{c.name}</b></td>
  <td>{c.company}</td>
  <td>{c.email}</td>
  <td>{c.phone}</td>
  <td>
    <button
      className="btn secondary"
      onClick={()=>{
        setEditingCustomer(c);
        setModal('editCustomer');
      }}
    >
      Edit
    </button>
  </td>
</tr>)}</tbody></table></div></>}
{tab==='Products'&&<><div className="row" style={{justifyContent:'space-between'}}>
  <div className="row">
    <b>Product catalog</b>
    <span className="muted">Price Guide sync will update this catalog; historical orders remain unchanged.</span>
  </div>
  <button className="btn" onClick={syncPriceGuide}>Sync Price Guide</button>
</div><div className="panel"><table><thead><tr><th>SKU</th><th>Product</th><th>Vendor</th><th>Category</th><th>Sell</th><th>Cost</th></tr></thead><tbody>{products.map(p=><tr key={p.id}><td>{p.sku}</td><td><b>{p.name}</b></td><td>{p.vendor}</td><td>{p.category}</td><td>${Number(p.sell_price).toLocaleString()}</td><td>{p.cost==null?'—':'$'+Number(p.cost).toLocaleString()}</td></tr>)}</tbody></table></div></>}
{tab==='Orders'&&<><button className="btn" onClick={()=>setModal('order')}>+ New Order</button><div className="panel"><table><thead><tr><th>Order</th><th>Reference #</th><th>Customer</th><th>Status</th><th>Total</th></tr></thead><tbody>{orders.map(o=><tr key={o.id} onClick={()=>{setSelected(o);setTab('Order')}} style={{cursor:'pointer'}}><td><b>{o.order_number}</b></td><td>{o.reference_number||'—'}</td><td>{(o.customers as any)?.name}</td><td>{o.status}</td><td>${totals(o).sell.toLocaleString()}</td></tr>)}</tbody></table></div></>}
{tab==='Purchasing'&&<PurchasingView orders={orders} updateItem={updateItem} createPurchaseOrder={createPurchaseOrder} openOrder={(o:any)=>{setSelected(o);setSelectedPO(null);setTab('Order')}}/>}
{tab==='Purchase Orders'&&<PurchaseOrdersView purchaseOrders={purchaseOrders} openPO={(po:any)=>{setSelectedPO(po);setSelected(null);setTab('Purchase Order')}}/>}
{tab==='Purchase Order'&&selectedPO&&<PurchaseOrderView po={selectedPO} back={()=>{setSelectedPO(null);setTab('Purchase Orders')}}/>}
{tab==='Order'&&selected&&<OrderView o={selected} products={products} totals={totals(selected)} addProduct={addProduct} addOther={()=>setModal('other')} openInvoice={openInvoice} dup={dup} del={del} updateItem={updateItem} updateShippingAddress={updateShippingAddress} updateReferenceNumber={updateReferenceNumber} updateOrderStatus={updateOrderStatus} createPurchaseOrder={createPurchaseOrder} purchaseOrders={purchaseOrders} openPO={(po:any)=>{setSelectedPO(po);setTab('Purchase Order')}} addOrderNote={addOrderNote}/>}
{tab==='Invoice'&&selected&&<InvoiceView o={selected} totals={totals(selected)} back={()=>setTab('Order')} addPayment={()=>setModal('payment')}/>} </div></main>
{modal&&<div className="modalbg" onMouseDown={()=>setModal('')}><div className="modal" onMouseDown={e=>e.stopPropagation()}>{modal==='customer'&&<form action={createCustomer}><h2>New Customer</h2><div className="grid">{['name','company','email','phone'].map(n=><div className="field" key={n}><label>{n}</label><input name={n} required={n==='name'}/></div>)}</div><button className="btn">Create Customer</button></form>} {modal==='editCustomer'&&editingCustomer&&
<form action={updateCustomer}>
  <h2>Edit Customer</h2>

  <input type="hidden" name="id" value={editingCustomer.id}/>

  <div className="grid">
    <div className="field">
      <label>Name</label>
      <input name="name" defaultValue={editingCustomer.name} required/>
    </div>

    <div className="field">
      <label>Company</label>
      <input name="company" defaultValue={editingCustomer.company||''}/>
    </div>

    <div className="field">
      <label>Email</label>
      <input name="email" type="email" defaultValue={editingCustomer.email||''}/>
    </div>

    <div className="field">
      <label>Phone</label>
      <input name="phone" defaultValue={editingCustomer.phone||''}/>
    </div>

    <div className="field">
      <label>Billing Address</label>
      <textarea name="billing_address" rows={3} defaultValue={editingCustomer.billing_address||''}/>
    </div>

    <div className="field">
      <label>Shipping Address</label>
      <textarea name="shipping_address" rows={3} defaultValue={editingCustomer.shipping_address||''}/>
    </div>
  </div>

  <div className="field">
    <label>Customer Notes</label>
    <textarea name="notes" rows={4} defaultValue={editingCustomer.notes||''}/>
  </div>

  <div className="row">
    <button className="btn">Save Customer</button>
    <button
      type="button"
      className="btn secondary"
      onClick={()=>{
        setModal('');
        setEditingCustomer(null);
      }}
    >
      Cancel
    </button>
  </div>
</form>}{modal==='order'&&<form action={createOrder}><h2>New Order</h2><div className="grid"><div className="field"><label>Customer</label><input list="cust" placeholder="Type to search" onChange={e=>{let c=customers.find(x=>x.name===e.target.value);let h=document.getElementById('cid') as HTMLInputElement;if(h)h.value=c?.id||''}}/><datalist id="cust">{customers.map(c=><option key={c.id} value={c.name}>{c.company||c.email}</option>)}</datalist><input id="cid" name="customer_id" type="hidden" required/></div><div className="field"><label>Reference #</label><input name="reference_number" placeholder="e.g. PO Mansell"/></div><div className="field"><label>Tax Rate %</label><input name="tax_rate" type="number" step=".01" defaultValue="7.45"/></div></div><p className="muted">Total is calculated from order items.</p><button className="btn">Create Order</button></form>}{modal==='other'&&<form action={addOther}><h2>Add Labor / Other</h2><div className="grid"><div className="field"><label>Type</label><select name="item_type"><option>Labor/Service</option><option>Shipping</option><option>Miscellaneous</option><option>Discount</option></select></div><div className="field"><label>Description</label><input name="description" required/></div><div className="field"><label>Qty</label><input name="qty" type="number" defaultValue="1"/></div><div className="field"><label>Sell Price</label><input name="sell_price" type="number" step=".01" required/></div><div className="field"><label>Internal Cost</label><input name="cost" type="number" step=".01" placeholder="Optional"/></div></div><button className="btn">Add Item</button></form>}{modal==='payment'&&selected&&<form action={addPayment}><h2>Record Payment</h2><div className="grid"><div className="field"><label>Amount</label><input name="amount" type="number" step=".01" min="0.01" required/></div><div className="field"><label>Payment Method</label><select name="payment_method"><option>Credit Card</option><option>ACH</option><option>Wire</option><option>Check</option><option>Cash</option><option>Other</option></select></div><div className="field"><label>Reference / Confirmation</label><input name="reference"/></div></div><div className="field"><label>Notes</label><input name="notes"/></div><button className="btn">Record Payment</button></form>}</div></div>}</div>}
function PurchasingView({orders,updateItem,createPurchaseOrder,openOrder}:any){const [showReceived,setShowReceived]=useState(false),[search,setSearch]=useState(''),[selectedIds,setSelectedIds]=useState<string[]>([]);const rows=orders.flatMap((o:any)=>(o.order_items||[]).filter((i:any)=>i.item_type==='Product').map((i:any)=>({item:i,order:o}))).filter((x:any)=>showReceived||x.item.purchasing_status!=='Received').filter((x:any)=>{const q=search.trim().toLowerCase();return !q||[x.order.order_number,x.order.reference_number,x.order.customers?.name,x.item.description,x.item.sku,x.item.vendor,x.item.purchasing_status,x.item.tracking].some((v:any)=>(v||'').toLowerCase().includes(q))});const counts=['Need to Order','Ordered','Backordered','Shipped','Received'].map(st=>({st,n:orders.flatMap((o:any)=>o.order_items||[]).filter((i:any)=>i.item_type==='Product'&&i.purchasing_status===st).length}));const chosen=rows.filter((x:any)=>selectedIds.includes(x.item.id));const canPO=chosen.length>0&&chosen.every((x:any)=>x.order.id===chosen[0].order.id&&x.item.vendor===chosen[0].item.vendor)&&!!chosen[0].order.reference_number;function toggle(x:any,checked:boolean){if(checked&&selectedIds.length){const first=rows.find((r:any)=>r.item.id===selectedIds[0]);if(first&&(first.order.id!==x.order.id||first.item.vendor!==x.item.vendor)){alert('Select items from the same Sales Order and vendor for one PO.');return}}setSelectedIds(v=>checked?[...new Set([...v,x.item.id])]:v.filter(id=>id!==x.item.id))}return <><div className="row" style={{justifyContent:'space-between',marginBottom:18}}><div><h1>Parts Purchasing</h1><div className="muted">Company-wide purchasing overview. Open an order to manage purchasing for one customer.</div></div><div className="row"><input placeholder="Search order, customer, part, SKU or vendor" value={search} onChange={e=>setSearch(e.target.value)} style={{width:360}}/><label className="row" style={{gap:7}}><input type="checkbox" checked={showReceived} onChange={e=>setShowReceived(e.target.checked)}/> Show Received</label></div></div><div className="cards">{counts.map(x=><div className="card" key={x.st}><div className="muted">{x.st}</div><div className="value">{x.n}</div></div>)}</div>{selectedIds.length>0&&<div className="panel"><div className="row" style={{justifyContent:'space-between'}}><div><b>{selectedIds.length} item{selectedIds.length===1?'':'s'} selected</b>{chosen[0]&&<div className="muted">{chosen[0].item.vendor} • {chosen[0].order.reference_number||'Reference # required'}</div>}</div><div className="row"><button className="btn secondary" onClick={()=>setSelectedIds([])}>Clear</button><button className="btn" disabled={!canPO} onClick={async()=>{await createPurchaseOrder(chosen[0].order,chosen.map((x:any)=>x.item));setSelectedIds([])}}>Create PO</button></div></div></div>}<div className="panel"><table><thead><tr><th></th><th>Order / Customer</th><th>Part</th><th>Vendor</th><th>Qty</th><th>Status</th><th>Tracking</th></tr></thead><tbody>{rows.map(({item:i,order:o}:any)=><tr key={i.id}><td><input type="checkbox" checked={selectedIds.includes(i.id)} onChange={e=>toggle({item:i,order:o},e.target.checked)}/></td><td><button className="btn ghost" onClick={()=>openOrder(o)}>{o.order_number}</button>{o.reference_number&&<div><b>{o.reference_number}</b></div>}<div className="muted">{o.customers?.name||'—'}</div></td><td><b>{i.description}</b>{i.sku&&<div className="muted">SKU: {i.sku}</div>}</td><td>{i.vendor||'—'}</td><td>{Number(i.qty)}</td><td><select value={i.purchasing_status} onChange={e=>updateItem(i,'purchasing_status',e.target.value)}>{['Need to Order','Ordered','Backordered','Shipped','Received'].map(x=><option key={x}>{x}</option>)}</select></td><td><input key={`${i.id}-${i.tracking||''}`} defaultValue={i.tracking||''} placeholder="Tracking #" onBlur={e=>{if(e.target.value!==(i.tracking||''))updateItem(i,'tracking',e.target.value||null)}} style={{minWidth:150}}/></td></tr>)}{rows.length===0&&<tr><td colSpan={7} className="muted">No purchasing items match this view.</td></tr>}</tbody></table></div></>}

function PurchaseOrdersView({purchaseOrders,openPO}:any){return <><div style={{marginBottom:18}}><h1>Purchase Orders</h1><div className="muted">Vendor POs created from Parts Purchasing.</div></div><div className="panel"><table><thead><tr><th>PO Number</th><th>Vendor</th><th>Sales Order</th><th>Customer</th><th>Date</th><th>Total Cost</th><th>Status</th></tr></thead><tbody>{purchaseOrders.map((po:any)=>{const total=(po.purchase_order_items||[]).reduce((a:number,i:any)=>a+Number(i.qty)*Number(i.unit_cost||0),0);return <tr key={po.id} onClick={()=>openPO(po)} style={{cursor:'pointer'}}><td><b>{po.po_number}</b></td><td>{po.vendor}</td><td>{po.orders?.order_number||'—'}</td><td>{po.orders?.customers?.name||'—'}</td><td>{new Date(po.created_at).toLocaleDateString()}</td><td>${total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td><td>{po.status}</td></tr>})}{purchaseOrders.length===0&&<tr><td colSpan={7} className="muted">No purchase orders yet.</td></tr>}</tbody></table></div></>}

function PurchaseOrderView({po,back}:any){const items=po.purchase_order_items||[],total=items.reduce((a:number,i:any)=>a+Number(i.qty)*Number(i.unit_cost||0),0);return <><div className="invoice-actions row"><button className="btn ghost" onClick={back}>← Back to Purchase Orders</button><button className="btn secondary" onClick={()=>window.print()}>Print / Save PDF</button></div><div className="invoice-sheet"><div className="invoice-head"><div><img src="/flatout-logo.svg"/><div style={{marginTop:10,lineHeight:1.45}}><b>Flatout Sim Racing</b><div>1388 S 300 W</div><div>Suite 100</div><div>Salt Lake City, UT 84115</div><div>info@driveflatout.com</div></div></div><div className="invoice-title"><h1>PURCHASE ORDER</h1><div><b>PO: {po.po_number}</b></div><div>Vendor: {po.vendor}</div><div className="muted">Date: {new Date(po.created_at).toLocaleDateString()}</div></div></div><div className="invoice-meta"><div><div className="invoice-label">VENDOR</div><b>{po.vendor}</b></div><div><div className="invoice-label">SALES ORDER</div><div>{po.orders?.order_number||'—'}</div><div>{po.orders?.customers?.name||''}</div></div><div><div className="invoice-label">SHIP TO</div><b>Flatout Sim Racing</b><div>1388 S 300 W, Suite 100</div><div>Salt Lake City, UT 84115</div></div></div><table className="invoice-table"><thead><tr><th>Description</th><th>SKU</th><th>Qty</th><th>Unit Cost</th><th>Amount</th></tr></thead><tbody>{items.map((i:any)=><tr key={i.id}><td><b>{i.description}</b></td><td>{i.sku||'—'}</td><td>{Number(i.qty)}</td><td>${Number(i.unit_cost||0).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td><td>${(Number(i.qty)*Number(i.unit_cost||0)).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td></tr>)}</tbody></table><div className="invoice-summary"><div className="invoice-total"><span>PO Total</span><b>${total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div></div>{po.notes&&<div className="panel"><b>Notes</b><div style={{whiteSpace:'pre-line',marginTop:6}}>{po.notes}</div></div>}<div className="invoice-footer">Flatout Sim Racing Purchase Order</div></div></>}

function OrderView({o,products,totals,addProduct,addOther,openInvoice,dup,del,updateItem,updateShippingAddress,updateReferenceNumber,updateOrderStatus,createPurchaseOrder,purchaseOrders,openPO,addOrderNote}:any){const [section,setSection]=useState('Order Items'),[pid,setPid]=useState(''),[productSearch,setProductSearch]=useState(''),[shipping,setShipping]=useState(o.shipping_address||''),[reference,setReference]=useState(o.reference_number||''),[selectedIds,setSelectedIds]=useState<string[]>([]),[note,setNote]=useState('');useEffect(()=>{setShipping(o.shipping_address||'');setReference(o.reference_number||'');setSelectedIds([])},[o.id,o.shipping_address,o.reference_number]);const q=productSearch.trim().toLowerCase(),matches=q?products.filter((p:any)=>[p.name,p.sku,p.vendor,p.category].some((v:any)=>(v||'').toLowerCase().includes(q))).slice(0,12):[];const physical=(o.order_items||[]).filter((i:any)=>i.item_type==='Product');const chosen=physical.filter((i:any)=>selectedIds.includes(i.id));const canPO=chosen.length>0&&chosen.every((i:any)=>i.vendor===chosen[0].vendor)&&!!chosen[0].vendor&&!!o.reference_number;const orderPOs=(purchaseOrders||[]).filter((po:any)=>po.order_id===o.id);const statuses=['Draft','Confirmed','Parts Ordering','Parts Ordered','Build Ready','Building','Ready for Installation','Completed'];function togglePOItem(i:any,checked:boolean){if(checked&&selectedIds.length){const first=physical.find((x:any)=>x.id===selectedIds[0]);if(first&&first.vendor!==i.vendor){alert('Select items from the same vendor for one PO.');return}}setSelectedIds(v=>checked?[...new Set([...v,i.id])]:v.filter(id=>id!==i.id))}return <><div className="row" style={{justifyContent:'space-between',alignItems:'flex-start'}}><div><h1>{o.order_number}</h1><div className="muted">{o.customers?.name}{o.reference_number?` • ${o.reference_number}`:''}</div></div><div className="row"><div className="field" style={{margin:0,minWidth:210}}><label>Order Status</label><select value={o.status} onChange={e=>updateOrderStatus(e.target.value)}>{statuses.map(x=><option key={x}>{x}</option>)}</select></div><button className="btn secondary" onClick={openInvoice}>View Invoice</button></div></div><div className="panel" style={{marginTop:18}}><div className="grid"><div className="field" style={{margin:0}}><label>Reference #</label><div className="row"><input value={reference} onChange={e=>setReference(e.target.value)} placeholder="e.g. PO Mansell"/><button className="btn secondary" onClick={()=>updateReferenceNumber(reference)}>Save</button></div></div><div className="field" style={{margin:0}}><label>Shipping Address</label><div className="row"><textarea value={shipping} onChange={e=>setShipping(e.target.value)} placeholder="Shipping address for this order" rows={2} style={{width:'100%',resize:'vertical'}}/><button className="btn secondary" onClick={()=>updateShippingAddress(shipping)}>Save</button></div></div></div></div><div className="cards" style={{marginTop:18}}><div className="card"><div className="muted">Sell</div><div className="value">${totals.sell.toLocaleString()}</div></div><div className="card"><div className="muted">Cost</div><div className="value">${totals.cost.toLocaleString()}</div></div><div className="card"><div className="muted">Gross Profit</div><div className="value">${totals.gp.toLocaleString()}</div></div><div className="card"><div className="muted">Gross Margin</div><div className="value">{totals.gm.toFixed(1)}%</div></div></div><div className="row" style={{gap:8,margin:'18px 0'}}>{['Order Items','Purchasing','Purchase Orders','Payments'].map(x=><button key={x} className={section===x?'btn':'btn secondary'} onClick={()=>setSection(x)}>{x}{x==='Purchase Orders'&&orderPOs.length?` (${orderPOs.length})`:''}</button>)}</div>{section==='Order Items'&&<><div className="row" style={{justifyContent:'space-between',marginBottom:12}}><h2 style={{margin:0}}>Order Items</h2><div className="row"><div style={{position:'relative',minWidth:330}}><input value={productSearch} onChange={e=>{setProductSearch(e.target.value);setPid('')}} placeholder="Search product, SKU, vendor or category..." style={{width:'100%'}}/>{q&&matches.length>0&&!pid&&<div style={{position:'absolute',zIndex:20,top:'100%',left:0,right:0,background:'#fff',border:'1px solid #dce0d9',maxHeight:300,overflowY:'auto',boxShadow:'0 8px 20px rgba(0,0,0,.12)'}}>{matches.map((p:any)=><button type="button" key={p.id} onClick={()=>{setPid(p.id);setProductSearch(p.name)}} style={{display:'block',width:'100%',textAlign:'left',padding:'10px 12px',background:'#fff',border:0,borderBottom:'1px solid #eee',cursor:'pointer'}}><b>{p.name}</b><div className="muted">{[p.sku,p.vendor,p.category].filter(Boolean).join(' • ')}</div></button>)}</div>}</div><button className="btn" disabled={!pid} onClick={()=>{if(pid){addProduct(pid);setPid('');setProductSearch('')}}}>+ Add Product</button><button className="btn secondary" onClick={addOther}>+ Labor / Other</button></div></div><div className="panel"><table><thead><tr><th>Item</th><th>Type</th><th>Qty</th><th>Sell</th><th>Cost</th><th>Status</th><th>Actions</th></tr></thead><tbody>{(o.order_items||[]).map((i:any)=><tr key={i.id}><td><b>{i.description}</b><div className="muted">{i.vendor}</div></td><td>{i.item_type}</td><td><input style={{width:60}} defaultValue={i.qty} onBlur={e=>updateItem(i,'qty',Number(e.target.value))}/></td><td><input style={{width:100}} defaultValue={i.sell_price} onBlur={e=>updateItem(i,'sell_price',Number(e.target.value))}/></td><td><input style={{width:100}} defaultValue={i.cost??''} placeholder="—" onBlur={e=>updateItem(i,'cost',e.target.value===''?null:Number(e.target.value))}/></td><td>{i.item_type==='Product'?<select value={i.purchasing_status} onChange={e=>updateItem(i,'purchasing_status',e.target.value)}>{['Need to Order','Ordered','Backordered','Shipped','Received'].map(x=><option key={x}>{x}</option>)}</select>:'—'}</td><td><div className="row"><button className="btn ghost" onClick={()=>dup(i)}>Duplicate</button><button className="btn ghost" onClick={()=>del(i)}>Remove</button></div></td></tr>)}</tbody></table></div></>}{section==='Purchasing'&&<><div className="row" style={{justifyContent:'space-between',marginBottom:12}}><div><h2 style={{margin:0}}>Purchasing</h2><div className="muted">Manage parts for this Sales Order and create vendor POs here.</div></div>{selectedIds.length>0&&<div className="row"><span className="muted">{chosen.length} selected • {chosen[0]?.vendor||'Vendor required'}</span><button className="btn secondary" onClick={()=>setSelectedIds([])}>Clear</button><button className="btn" disabled={!canPO} onClick={async()=>{await createPurchaseOrder(o,chosen);setSelectedIds([])}}>Create PO</button></div>}</div>{!o.reference_number&&<div className="panel"><b>Reference # required for Purchase Orders.</b><div className="muted">Add a Reference # above before creating a vendor PO.</div></div>}<div className="panel"><table><thead><tr><th></th><th>Part</th><th>SKU</th><th>Vendor</th><th>Qty</th><th>Status</th><th>Tracking</th></tr></thead><tbody>{physical.map((i:any)=><tr key={i.id}><td><input type="checkbox" checked={selectedIds.includes(i.id)} onChange={e=>togglePOItem(i,e.target.checked)}/></td><td><b>{i.description}</b></td><td>{i.sku||'—'}</td><td>{i.vendor||'—'}</td><td>{Number(i.qty)}</td><td><select value={i.purchasing_status} onChange={e=>updateItem(i,'purchasing_status',e.target.value)}>{['Need to Order','Ordered','Backordered','Shipped','Received'].map(x=><option key={x}>{x}</option>)}</select></td><td><input key={`${i.id}-${i.tracking||''}`} defaultValue={i.tracking||''} placeholder="Tracking #" onBlur={e=>{if(e.target.value!==(i.tracking||''))updateItem(i,'tracking',e.target.value||null)}} style={{minWidth:170}}/></td></tr>)}{physical.length===0&&<tr><td colSpan={7} className="muted">No physical products on this order.</td></tr>}</tbody></table></div></>}{section==='Purchase Orders'&&<div className="panel"><h2>Purchase Orders</h2><table><thead><tr><th>PO Number</th><th>Vendor</th><th>Date</th><th>Total Cost</th><th>Status</th></tr></thead><tbody>{orderPOs.map((po:any)=>{const total=(po.purchase_order_items||[]).reduce((a:number,i:any)=>a+Number(i.qty)*Number(i.unit_cost||0),0);return <tr key={po.id} onClick={()=>openPO(po)} style={{cursor:'pointer'}}><td><b>{po.po_number}</b></td><td>{po.vendor}</td><td>{new Date(po.created_at).toLocaleDateString()}</td><td>${total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td><td>{po.status}</td></tr>})}{orderPOs.length===0&&<tr><td colSpan={5} className="muted">No Purchase Orders have been created for this Sales Order yet.</td></tr>}</tbody></table></div>}{section==='Payments'&&<div className="panel"><div className="row" style={{justifyContent:'space-between'}}><div><h2>Payments</h2><div className="muted">Payment status: {o.payment_status}</div></div><button className="btn" onClick={openInvoice}>Open Invoice / Record Payment</button></div><table><thead><tr><th>Date</th><th>Method</th><th>Reference</th><th>Notes</th><th>Amount</th></tr></thead><tbody>{(o.payments||[]).map((p:any)=><tr key={p.id}><td>{new Date(p.paid_at).toLocaleDateString()}</td><td>{p.payment_method||'—'}</td><td>{p.reference||'—'}</td><td>{p.notes||'—'}</td><td>${Number(p.amount).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td></tr>)}{!(o.payments||[]).length&&<tr><td colSpan={5} className="muted">No payments recorded yet.</td></tr>}</tbody></table></div>}<div className="panel" style={{marginTop:18}}><h2>Order Notes</h2><div style={{maxHeight:320,overflowY:'auto',marginBottom:18}}>{[...(o.order_notes||[])].sort((a:any,b:any)=>new Date(b.created_at).getTime()-new Date(a.created_at).getTime()).map((n:any)=><div key={n.id} style={{padding:'12px 0',borderBottom:'1px solid #eee'}}><div style={{whiteSpace:'pre-line'}}>{n.note}</div><div className="muted" style={{marginTop:5}}>{new Date(n.created_at).toLocaleString()}</div></div>)}{!(o.order_notes||[]).length&&<div className="muted">No order notes yet.</div>}</div><div className="field"><label>Add Order Note</label><textarea value={note} onChange={e=>setNote(e.target.value)} rows={3} placeholder="Backorder, ETA, vendor update, customer update, installation note, etc."/></div><button className="btn" disabled={!note.trim()} onClick={async()=>{await addOrderNote(note);setNote('')}}>Add Note</button></div></>}

function InvoiceView({o,totals,back,addPayment}:any){const tax=totals.sell*Number(o.tax_rate||0)/100,total=totals.sell+tax,paid=(o.payments||[]).reduce((a:number,p:any)=>a+Number(p.amount),0),balance=Math.max(0,total-paid);return <><div className="invoice-actions row"><button className="btn ghost" onClick={back}>← Back to Order</button><button className="btn secondary" onClick={()=>window.print()}>Print / Save PDF</button><button className="btn" onClick={addPayment}>+ Record Payment</button></div><div className="invoice-sheet"><div className="invoice-head"><div><img src="/flatout-logo.svg"/><div style={{marginTop:10,lineHeight:1.45}}><b>Flatout Sim Racing</b><div>1388 S 300 W</div><div>Suite 100</div><div>Salt Lake City, UT 84115</div><div>info@driveflatout.com</div></div></div><div className="invoice-title"><h1>INVOICE</h1><div><b>{o.order_number}</b></div>{o.reference_number&&<div>Reference: {o.reference_number}</div>}<div className="muted">Status: {o.payment_status}</div></div></div><div className="invoice-meta"><div><div className="invoice-label">BILL TO</div><b>{o.customers?.name}</b>{o.customers?.company&&<div>{o.customers.company}</div>}{o.customers?.email&&<div>{o.customers.email}</div>}{o.customers?.phone&&<div>{o.customers.phone}</div>}{o.customers?.billing_address&&<div>{o.customers.billing_address}</div>}</div>{o.shipping_address&&<div><div className="invoice-label">SHIP TO</div><div style={{whiteSpace:'pre-line'}}>{o.shipping_address}</div></div>}<div><div className="invoice-label">INVOICE DATE</div><div>{new Date(o.created_at||Date.now()).toLocaleDateString()}</div></div></div><table className="invoice-table"><thead><tr><th>Description</th><th>Qty</th><th>Unit Price</th><th>Amount</th></tr></thead><tbody>{(o.order_items||[]).map((i:any)=><tr key={i.id}><td><b>{i.description}</b>{i.sku&&<div className="muted">SKU: {i.sku}</div>}</td><td>{Number(i.qty)}</td><td>${Number(i.sell_price).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td><td>${(Number(i.qty)*Number(i.sell_price)).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td></tr>)}</tbody></table><div className="invoice-summary"><div><span>Subtotal</span><b>${totals.sell.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div><div><span>Tax ({Number(o.tax_rate||0).toFixed(2)}%)</span><b>${tax.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div><div className="invoice-total"><span>Total</span><b>${total.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div><div><span>Paid</span><b>${paid.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div><div className="invoice-balance"><span>Balance Due</span><b>${balance.toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</b></div></div>{(o.payments||[]).length>0&&<div className="payment-history"><h2>Payments</h2><table><thead><tr><th>Date</th><th>Method</th><th>Reference</th><th>Amount</th></tr></thead><tbody>{o.payments.map((p:any)=><tr key={p.id}><td>{new Date(p.paid_at).toLocaleDateString()}</td><td>{p.payment_method||'—'}</td><td>{p.reference||'—'}</td><td>${Number(p.amount).toLocaleString(undefined,{minimumFractionDigits:2,maximumFractionDigits:2})}</td></tr>)}</tbody></table></div>}<div className="invoice-footer">Thank you for choosing Flatout Sim Racing.</div></div></>}
